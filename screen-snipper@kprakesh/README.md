# Screen Snipper

**UUID:** `screen-snipper@kprakesh`

A simple, lightweight Cinnamon applet that allows you to quickly capture screenshots directly from your panel. It supports selecting specific areas or taking full-screen captures with a customizable delay.

## Features

* **✂️ Capture Area:** innovative "snip" tool to select a region of your screen and copy it directly to the clipboard.
* **⏱️ Delayed Capture:** Take a full-screen screenshot after a set delay (perfect for capturing open menus or tooltips).
* **⚙️ Configurable:** Customize the delay time (in seconds) via the applet settings.
* **📋 Clipboard Integration:** All captures are automatically copied to your clipboard, ready to paste.
* **🎨 Native Look:** Uses standard system icons to blend perfectly with your Linux Mint theme.

## Dependencies

This applet relies on the standard GNOME screenshot tool. It is pre-installed on most Linux Mint systems, but you can verify it is installed by running:

```bash
sudo apt install gnome-screenshot
