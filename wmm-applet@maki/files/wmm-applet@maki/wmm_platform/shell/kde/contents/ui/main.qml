import QtQuick 2.0

Item {
    // Este componente no pinta nada visualmente.
    // Su única función es registrar a WMM como un tipo de fondo válido.
    // El control real lo tiene el motor de WMM a través de plasma-apply-wallpaperimage.
}
