{
	"dockerComposeProjectFolder": {
		"type": "filechooser",
		"default": "~/docker_projects",
		"description": "Docker Compose base project folder",
		"select-dir": true,
        "allow-none" : false,
		"tooltip": "This is the base folder for your docker-compose YAML files. This applet will scan through each sub-folder looking for configuration files."
	},
	"dockerComposeCmd": {
		"type": "filechooser",
		"default": "docker-compose",
		"description": "Docker Compose command location",
        "allow-none" : false,
		"tooltip": "This is the command that is run to start Docker Compose."
	},
	"dockerCmd": {
		"type": "filechooser",
		"default": "docker",
		"description": "Docker command location",
        "allow-none" : false,
		"tooltip": "This is the command that is run to start Docker."
	},
	"editor": {
		"type": "filechooser",
		"default": "xed",
		"description": "Default text editor",
        "allow-none" : false,
		"tooltip": "This is the default text editor for editing configuration files."
	}
}
